import classes from "@/styles/Logo.module.css";

/**
 * ロゴ.
 *
 * @returns {JSX.Element} ロゴ.
 */
const Logo = () => {
  return <span className={classes.logo}>YumeYobi</span>;
};

export default Logo;
